change the custom decorate idea - instead of decorating around the quote, provide a doodle prompt instead. 

## prompt guidelines:
- 1 randomly generated doodle prompt a day.
- the prompt should be a single word of a random object.
- the same randomly generated palettes should stay. 

## UI guidelines:
- the prompt should be at the top, and a rounded vertical rectangle below it, indicating the canvas. 
- all drawing should only be allowed within the canvas.
- the move tool is no longer relevant. 

## backend guidelines:
- the saving should include the prompt (instead of the current message theme), and the am/pm should be removed since it is 1 prompt per day.